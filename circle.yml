dependencies:
  pre:
    - npm i
    - sh login.sh

test:
  override:
    - npm test

deployment:
  production:
    tag: /release-.*/
    branches:
      ignore: *
    commands:
      - npm publish ./